<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light_gray"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/btnAddFolder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+ 폴더 추가"
        android:textColor="@color/main_red"
        android:padding="8dp" />

    <TextView
        android:id="@+id/tvGuideMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_guide_box"
        android:gravity="center"
        android:text="👆폴더를 길게 누르면 순서 변경\n👈폴더를 왼쪽으로 스와이프하면 하위폴더로 변경\n👉폴더를 오른쪽으로 스와이프하면 상위폴더로 변경"
        android:textColor="#ff3737"
        android:textSize="12sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFolderList"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>